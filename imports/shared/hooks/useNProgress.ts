/**
 * Hook that previously showed progress bar during route changes
 * Now it's just an empty hook that returns null
 */
export const useNProgress = () => {
  return null;
};

export default useNProgress;

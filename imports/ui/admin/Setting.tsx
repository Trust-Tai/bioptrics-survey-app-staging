import React from 'react';
import AdminLayout from './AdminLayout';

const Setting: React.FC = () => {
  return (
    <AdminLayout>
      <div style={{ maxWidth: 600, margin: '0 auto', background: '#fff', borderRadius: 12, padding: 32, marginTop: 40 }}>
        {/* Empty content for Settings landing page */}
      </div>
    </AdminLayout>
  );
};

export default Setting;
